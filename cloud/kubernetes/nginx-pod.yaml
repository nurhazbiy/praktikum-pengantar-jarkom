apiVersion: v1 
kind: Pod 
metadata:
  name: hello-pod 
  labels:
    zone: prod
    version: v1 
spec:
  containers:
    - name: hello-web
      image: nginx:alpine 
      ports:
      - containerPort: 80